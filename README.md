# GDSQL

Godot 引擎的数据库 SQL 工作台插件 / Database SQL Workbench plugin for Godot Engine.

基于 Godot 的 `ConfigFile` 机制，提供可视化的数据库浏览、SQL 查询、数据编辑等功能。

## 功能

- 可视化数据库浏览
- SQL 查询与数据编辑
- 数据表结构管理
- 支持加密数据库
- XML 编辑器
- GBatis 映射框架

## 使用

将本仓库放到项目的 `addons/` 目录下，然后在 Godot Editor 的项目设置中启用 GDSQL 插件。
